# Publishes site/ (the weekly card) to GitHub Pages:
# https://danielregaladoumiami.github.io/canes-cfb-analytics/
# Runs when site/ changes on main, after every weekly-predictions run (its commits are
# pushed with GITHUB_TOKEN, which doesn't trigger push events), or by hand.
name: pages

on:
  push:
    branches: [main]
    paths: ["site/**"]
  workflow_run:
    workflows: [weekly-predictions]
    types: [completed]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    if: github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site
      - id: deployment
        uses: actions/deploy-pages@v4
